// firebase.js
import { initializeApp } from 'firebase/app';

const firebaseConfig = {
  // YProject name



projectId: 'fin21-cef3d',

};

const firebaseApp = initializeApp(firebaseConfig);

export default firebaseApp;
